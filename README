simple console scrobbler to Last.fm

usage: lsrobbler.py <fname>
if no file in command line, all *mp3 files will be scrobbled

for property working need to put login and password in separate config file
